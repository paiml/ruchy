{ x: 1, y: 2 }.x

let obj = { name: "test", data: { count: 42 } }
obj.name
obj.data

[1, 2, 3][0]
[10, 20, 30][1]

let arr = [[1, 2], [3, 4], [5, 6]]
arr[0]
arr[1][1]

{ users: [{ name: "Alice", id: 1 }, { name: "Bob", id: 2 }] }.users[0].name

let compute = fun(x) { x * 2 + 1 }
[1, 2, 3, 4].map(compute)

(fun(x) { x + 10 })(5)

let nested_fn = fun(a) { fun(b) { a + b } }
let add5 = nested_fn(5)
add5(3)