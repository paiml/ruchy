//! RHL-0 grammar gate — RED until the grammar artifacts are pre-registered.

#[test]
fn test_rhl_0_grammar_gate_is_pre_registered() {
    panic!("RHL-0: the grammar artifacts are not pre-registered yet");
}
