//! RHL-0 breaks gate — RED until the breaks artifacts are pre-registered.

#[test]
fn test_rhl_0_breaks_gate_is_pre_registered() {
    panic!("RHL-0: the breaks artifacts are not pre-registered yet");
}
