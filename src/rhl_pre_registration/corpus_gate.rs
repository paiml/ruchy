//! RHL-0 corpus gate — RED until the corpus artifacts are pre-registered.

#[test]
fn test_rhl_0_corpus_gate_is_pre_registered() {
    panic!("RHL-0: the corpus artifacts are not pre-registered yet");
}
