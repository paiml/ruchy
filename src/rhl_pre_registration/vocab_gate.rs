//! RHL-0 vocab gate — RED until the vocab artifacts are pre-registered.

#[test]
fn test_rhl_0_vocab_gate_is_pre_registered() {
    panic!("RHL-0: the vocab artifacts are not pre-registered yet");
}
