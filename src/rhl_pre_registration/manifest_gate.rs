//! RHL-0 manifest gate — RED until the manifest artifacts are pre-registered.

#[test]
fn test_rhl_0_manifest_gate_is_pre_registered() {
    panic!("RHL-0: the manifest artifacts are not pre-registered yet");
}
