fn main() {
    let p = true;
    let q = false;
    println!("AND: {} && {} = {}", p, q, p && q);
    println!("OR: {} || {} = {}", p, q, p || q);
    println!("NOT: !{} = {}", p, ! p);
    println!("NOT: !{} = {}", q, ! q);
}
