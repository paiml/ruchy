# Z0 / PMAT-102 verification (orchestrator-run, worktree /home/noah/src/ruchy-wt-PMAT-102, CARGO_TARGET_DIR=/mnt/nvme-raid0/targets/ruchy)

## Before the fix (HEAD 3a665f36, rustc 1.98.0)
cargo clippy --all-targets --keep-going -- -D warnings  -> exit 101
  17 error: unused return value of `assert_cmd::Command::assert` that must be used   (+7 in tests/cli_contract_fuzz.rs reported by the hook first)
   4 error[E0063]: missing field `contracts` in initializer of `ruchy::Expr`         (benches/transpiler.rs:12,22,32,42)
cargo fmt --all -- --check -> exit 1, 20 files with diffs (rustfmt 1.98 vs the August toolchain); after `cargo fmt --all`, 4 files remained
  non-idempotent (proptest! bodies): tests/{wasm_memory_property_tests,property_enum_cast,issue_128_property_tests,issue_119_property_tests}.rs
  — exactly the files .rustfmt.toml lists under the nightly-only `ignore`.
clap prefix check (debug bin): --iterations -> 0, --iteration -> 2, --iterationz -> 2, --bogus -> 2

## After the fix
cargo fmt --all -- --check -> CLEAN
cargo clippy --all-targets --keep-going -- -D warnings -> exit 0
cargo test --test cli_contract_fuzz --test cli_contract_mutations --test cli_contract_property_tests --test stdlib_file_io_advanced_test
  16 passed 2 ignored / 13 passed 2 ignored / 18 passed / 11 passed -> exit 0

## DoD mutation (observed RED)
perl: first `--iterations` after the first `assert_args_accepted(` -> `--iterationz`
cargo test --test cli_contract_fuzz ->
  assertion `left != right` failed: CLI rejected its arguments (clap usage error): Output { status: ExitStatus(unix_wait_status(512)), stdout: "",
  stderr: "error: unexpected argument '--iterationz' found\n\n  tip: a similar argument exists: '--iterations'\n\nUsage: ruchy fuzz --iterations <ITERATIONS> <TARGET>\n..." }
  test result: FAILED. 15 passed; 1 failed; 2 ignored  -> exit 101
restored from backup; wraps=7, iterationz=0

## Note
An earlier mutation attempt replaced the FIRST `--iterations` in the file, which sits in an unwrapped test that asserts `.failure()`; it stayed green — that site is not covered by the helper and is not one of the 24. Recorded so the receipt does not overstate coverage.

## Correction after measuring CI's toolchain
CI's lint job runs in `localhost:5000/sovereign-ci:stable@sha256:66016caa…` and the last main run (31494372313, 2026-08-11) reports
`rustc 1.97.1 (8bab26f4f 2026-07-14)`. The fmt drift above is therefore a LOCAL rustc-1.98 artifact: a 1.98-formatted tree would
fail CI's 1.97.1 `cargo fmt --all -- --check`. The tree-wide `cargo fmt --all` and the `#[rustfmt::skip]` edits were dropped from Z0.
The pre-commit complexity gate independently refused the fmt-only touches of four files because they hold six pre-existing
functions over the per-function limits (cognitive 34–64; cyclomatic 35) — recorded as B20 in the plan.
Z0 = 24 must_use sites + tests/support/mod.rs + benches/transpiler.rs, verified under both rustc 1.98 (local stable) and 1.97.1 (CI).

## Z0b / PMAT-103 (worker opus aa120d044a3ef3ab2, then orchestrator re-run)
Six functions over the hook's per-function limits (the brief named five; the worker found resolve_qualified_name at cognitive 68 with the hook's own command):
  is_identifier_usage 18/34 -> 3/6 · handle_provability_command 35/64 -> 3/2 · handle_suggest_contracts 9/36 -> 5/4 · call_function 15/37 -> 8/8 · eval_type_cast 8/36 -> 3/4 · resolve_qualified_name 13/68 -> 4/11
Orchestrator re-run: complexity gate 0 violations on all four files; RED proof on `git show main:<file>` copies = 1/3/1/3 violations; fmt CLEAN (1.97.1 and 1.98); clippy exit 0; cargo test --lib 20510 passed.
Worker-run differential: examples/ 154 files, HEAD release vs new debug binary run stdout+exit: 153 identical, 1 differs (09_async_await.ruchy) — shown to differ run-to-run on the unmodified binary too (HashMap field order).
Commit 34a53fa6 (with PMAT-102).

## Z0c / PMAT-104 (worker opus a32af0dbc875a35fe partial, finished by orchestrator)
Bin unit-test target: at main it never completed (test_handle_watch_mode_setup -> thread -> process::exit(0) after 10 ms; cargo saw exit 0 after 639-666 of 952 tests).
After removing it the target exposed: 2 non-hermetic /nonexistent tests (the dir exists on this host), 1 case-sensitive assertion, 1 wrong expectation (empty program), 18 blocking tests (11 prover interactive loop on process stdin, 4 watch-mode infinite loop, 3 dispatch tests that scanned the repo / started the notebook server / ran cargo add), and, once those ran, a cwd race across 17 tests in build.rs/add.rs/test.rs.
Orchestrator re-run after fixes: cargo test -p ruchy --bin ruchy -> 954 passed 0 failed 0 ignored (parallel 1.73 s; --test-threads=1 5.2 s); clippy exit 0; fmt CLEAN both toolchains; complexity gate 0 violations on every staged file (get_latest_modification 28 -> split). No #[ignore] added. Commit e9d8365e.
Side measurement: `ruchy prove < /dev/null` -> 4.2.1 exit 124 (hang), HEAD exit 0.

## Z0d / PMAT-094 item (a) (orchestrator)
Cargo.toml [dev-dependencies] provable-contracts = "0.3.1" (crates.io). cargo check --tests -p ruchy exit 0 (tests/contract_traits.rs is the only consumer). cargo +1.97.1 fmt --all -- --check CLEAN. Root cause it removes: CI's `cargo fmt --all -- --check` also formats path dependencies, and the freshly cloned sibling fails rustfmt in the 1.97.1 container (…/provable-contracts/examples/doc_integrity.rs) — every PR's ci/lint was red for that. Commit b2d3557b. PR #204.
